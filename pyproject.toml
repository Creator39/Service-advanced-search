[project]
name = "microservice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "elasticsearch>=9.2.1",
    "fastapi[standard]>=0.128.0",
    "pika>=1.3.2",
    "pydantic>=2.12.5",
]
